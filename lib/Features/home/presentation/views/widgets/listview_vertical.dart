import 'package:bookly/Features/home/presentation/views/widgets/item_vertical.dart';
import 'package:flutter/material.dart';

class ListViewVertical extends StatelessWidget {
  const ListViewVertical({super.key});
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        return const ItemVertical();
      },
    );
  }
}

class ListViewVerticala extends StatelessWidget {
  const ListViewVerticala({super.key});
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        return const ItemVertical();
      },
    );
  }
}

class ListViewVerticalb extends StatelessWidget {
  const ListViewVerticalb({super.key});
  @override
  Widget build(BuildContext context) {
    return ListView.builder(
      itemBuilder: (context, index) {
        return const ItemVertical();
      },
    );
  }
}
